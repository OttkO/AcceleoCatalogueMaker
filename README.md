# AcceleoCatalogueMaker
Small script that adds all the queries and templates from a directory filled with *.mtl files
to a dictionary. Python3 is used. Input parameter: the directory that you want to examine.